#ifndef BSTNODE_H
#define BSTNODE_H


class BSTnode
{
    public:
        int val;
        BSTnode* left;
        BSTnode* right;
        int lcount;
        BSTnode(int);

    protected:

    private:
};

#endif // BSTNODE_H
